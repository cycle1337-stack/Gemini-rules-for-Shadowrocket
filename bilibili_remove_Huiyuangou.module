#!name=B站去除会员购模块
#!desc=找回底部发布按钮，移除会员购，不干扰评论区，不报错。

[MITM]
hostname = %APPEND% app.bilibili.com, grpc.biliapi.net, api.bilibili.com

[Body Rewrite]
# [span_5](start_span)使用 JQ 精准重写底部 Tab，强制保留：首页、动态、发布(+)、我的[span_5](end_span)
# 逻辑：删除 pos 为 4 (通常是会员购) 的元素
http-response-jq ^https:\/\/app\.bilibili\.com\/x\/resource\/show\/tab\/v2\? '.data.bottom |= map(select(.pos != 4))'
