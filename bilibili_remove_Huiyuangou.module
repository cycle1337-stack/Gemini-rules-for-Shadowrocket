#!name=B站去除会员购模块
#!desc=找回底部发布按钮，移除会员购，不干扰评论区，不报错。

[MITM]
hostname = %APPEND% app.bilibili.com, grpc.biliapi.net, api.bilibili.com

[Script]
# 同时剔除 pos 3 (发布) 和 pos 4 (会员购)
B站底栏精简 = type=http-response, pattern=^https:\/\/app\.bilibili\.com\/x\/resource\/show\/tab\/v2, requires-body=1, binary-body-mode=1, jq='.data.bottom |= map(select(.pos != 3 and .pos != 4))'
